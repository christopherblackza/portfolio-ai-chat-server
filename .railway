{
  "docker": {
    "dockerfile": "Dockerfile"  // Path to the Dockerfile in your repository
  },
  "build": "npm run build",  // Defines a custom build command
  "start": "npm run start:prod"  // Start command to run after build
}